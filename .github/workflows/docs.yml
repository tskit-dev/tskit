name: Build Docs
on:
  pull_request:
  merge_group:
  push:
    branches: [main]
    tags:
      - '*'

env:
  FORCE_COLOR: 1

jobs:
  docs:
    uses: tskit-dev/.github/.github/workflows/docs.yml@v6
    with:
      pyproject-directory: python
